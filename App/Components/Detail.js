import React, {useEffect, useState} from 'react';
import {
    FlatList,
    StyleSheet, Text,
    View,
} from 'react-native';

export default Detail = (item) => {

    return (
        <View>

        </View>
    );
};

const styles = StyleSheet.create({});
